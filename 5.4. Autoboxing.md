# 🔥 Autoboxing in Java

## 📌 1. Definition

**Autoboxing** is the **automatic conversion of a primitive data type into its corresponding wrapper class object**.

👉 Introduced in **Java 5**

---

## 📌 2. Primitive → Wrapper Mapping

| Primitive | Wrapper Class |
| --------- | ------------- |
| `int`     | `Integer`     |
| `double`  | `Double`      |
| `char`    | `Character`   |
| `boolean` | `Boolean`     |
| `long`    | `Long`        |
| `float`   | `Float`       |
| `byte`    | `Byte`        |
| `short`   | `Short`       |

---

## 📌 3. Basic Example

```java
class Test {
    public static void main(String[] args) {
        int a = 10;

        Integer obj = a;  // autoboxing

        System.out.println(obj);
    }
}
```

---

## ✅ Output

```
10
```

---

## 💡 Explanation

* `int a = 10` → primitive
* `Integer obj = a` → automatically converted to `Integer` object
* Compiler internally does:

```java
Integer obj = Integer.valueOf(a);
```

---

# 📌 4. Unboxing (Reverse of Autoboxing)

## ✅ Definition

Converting **wrapper object → primitive**

---

### 📌 Example

```java
class Test {
    public static void main(String[] args) {
        Integer obj = 20;

        int a = obj;  // unboxing

        System.out.println(a);
    }
}
```

---

## 💡 Explanation

Internally:

```java
int a = obj.intValue();
```

---

# 📌 5. Autoboxing in Method Calls

---

### 📌 Example

```java
class Test {

    static void show(Integer x) {
        System.out.println(x);
    }

    public static void main(String[] args) {
        int a = 5;
        show(a);  // autoboxing
    }
}
```

---

## 💡 Explanation

* `int` automatically converted to `Integer`

---

# 📌 6. Autoboxing in Collections (VERY IMPORTANT)

---

### 📌 Example

```java
import java.util.*;

class Test {
    public static void main(String[] args) {
        ArrayList<Integer> list = new ArrayList<>();

        list.add(10);  // autoboxing
        list.add(20);

        System.out.println(list);
    }
}
```

---

## 💡 Explanation

* Collections store **objects only**
* Primitive `int` is converted to `Integer`

---

# 📌 7. Autoboxing with Method Overloading (Tricky)

---

### 📌 Example

```java
class Test {

    void show(int x) {
        System.out.println("int");
    }

    void show(Integer x) {
        System.out.println("Integer");
    }

    public static void main(String[] args) {
        new Test().show(10);
    }
}
```

---

## ✅ Output

```
int
```

---

## 💡 Explanation

* Exact match (`int`) is preferred over autoboxing

---

# 📌 8. NullPointerException (VERY IMPORTANT)

---

### 📌 Example

```java
class Test {
    public static void main(String[] args) {
        Integer obj = null;

        int x = obj; // ❌ Exception
    }
}
```

---

## ❌ Output

```
NullPointerException
```

---

## 💡 Explanation

* Unboxing requires actual value
* `null` cannot be converted to primitive

---

# 📌 9. Performance Consideration

---

## ⚠️ Theory

* Autoboxing creates **objects**
* More memory + slower than primitives

👉 Avoid in performance-critical code

---

# 📌 10. Key Points

---

* Automatic conversion (primitive → wrapper)
* Introduced in Java 5
* Works with collections
* Used in method overloading
* Can cause **NullPointerException**

---

# 🎯 Interview Summary (Perfect Answer)

> Autoboxing in Java is the automatic conversion of primitive data types into their corresponding wrapper class objects. It simplifies code by eliminating manual conversions and is widely used in collections and method calls. However, it can introduce performance overhead and may lead to NullPointerException during unboxing if the object is null.

---
